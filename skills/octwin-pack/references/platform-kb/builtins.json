{
  "version": "1.0",
  "builtins": [
    {
      "name": "concat",
      "category": "utility",
      "summary": "Concatenate arrays into one.",
      "description": "Concatenate arrays into one. Non-array args are appended as single\nelements; nullish args are skipped. Useful for prepending a sentinel row.\n  $concat([any_row], $map($rows, picker))   → [any_row, ...rows]",
      "examples": [
        "$concat([any_row], $map($rows, picker))   → [any_row, ...rows]"
      ],
      "signature": "(...parts: unknown[]): unknown[]"
    },
    {
      "name": "includes",
      "category": "utility",
      "summary": "Membership test.",
      "description": "Membership test. `$includes(collection, item)`:\n  • array      → true when an element strictly equals `item`\n  • string     → true when `item` is a substring\n  • otherwise  → false\n  $includes($amenities, 'pool')     · $includes($title, 'villa')",
      "examples": [
        "$includes($amenities, 'pool')     · $includes($title, 'villa')"
      ],
      "signature": "(collection: unknown, item: unknown): boolean"
    },
    {
      "name": "in",
      "category": "utility",
      "summary": "Reversed-argument membership — reads naturally as `item in collection`:\n  `$in($status, ['available', 'reserved'])`  → status is one of …\nSame matching rules as `$includes` (array element / string substring).",
      "description": "Reversed-argument membership — reads naturally as `item in collection`:\n  `$in($status, ['available', 'reserved'])`  → status is one of …\nSame matching rules as `$includes` (array element / string substring).",
      "examples": [],
      "signature": "(item: unknown, collection: unknown): boolean"
    },
    {
      "name": "filter_gte",
      "category": "utility",
      "summary": "Keep array items whose numeric `field` is >= `min`.",
      "description": "Keep array items whose numeric `field` is >= `min`. Items missing the\nfield or with a non-number are dropped. Generic threshold filter (the\ncomparison `$filter` — which only does equality — can't express).\n  $filter_gte($results, 'similarity', 0.35)",
      "examples": [
        "$filter_gte($results, 'similarity', 0.35)"
      ],
      "signature": "(arr: unknown, field: unknown, min: unknown): unknown[]"
    },
    {
      "name": "compact",
      "category": "utility",
      "summary": "Drop null / undefined elements from an array (keeps '', 0, false).",
      "description": "Drop null / undefined elements from an array (keeps '', 0, false).\n  $compact([a, cond ? b : null, c])  → the present values, in order\nReturns [] for non-array inputs.",
      "examples": [
        "$compact([a, cond ? b : null, c])  → the present values, in order"
      ],
      "signature": "(arr: unknown): unknown[]"
    },
    {
      "name": "slice",
      "category": "utility",
      "summary": "`Array.prototype.slice`.",
      "description": "`Array.prototype.slice`. Strings supported too. Defensive on non-array/string.",
      "examples": [],
      "signature": "(v: unknown, start: unknown, end?: unknown): unknown"
    },
    {
      "name": "take",
      "category": "utility",
      "summary": "First N elements of an array (or characters of a string).",
      "description": "First N elements of an array (or characters of a string).",
      "examples": [],
      "signature": "(v: unknown, n: unknown): unknown"
    },
    {
      "name": "map",
      "category": "iteration",
      "summary": "Map each element of `arr` to a new value.",
      "description": "Map each element of `arr` to a new value.\n  • `$map(arr, 'name')`               → arr.map(item => item.name)\n  • `$map(arr, 'a.b')`                → arr.map(item => item.a.b)        (dotted paths)\n  • `$map(arr, { id: 'id',            → arr.map(item => ({ id, label }))\n                 label: 'name_ar' })`    (object pattern → shape projection)\nReturns [] for non-array inputs.",
      "examples": [],
      "signature": "(arr: unknown, mapping: unknown): unknown[]"
    },
    {
      "name": "filter",
      "category": "iteration",
      "summary": "Filter elements.",
      "description": "Filter elements.\n  • `$filter(arr, 'is_active')`            → truthy check on item.is_active\n  • `$filter(arr, { kind: 'apartment' })`  → equality match on each pair\nReturns [] for non-array inputs.",
      "examples": [],
      "signature": "(arr: unknown, predicate: unknown): unknown[]"
    },
    {
      "name": "group_by",
      "category": "iteration",
      "summary": "Group elements by the value of `key` (or dotted path).",
      "description": "Group elements by the value of `key` (or dotted path).\n  • `$group_by(properties, 'compound')`   → { 'Mivida': [...], 'Hyde Park': [...] }\nReturns {} for non-array / non-string-key inputs.",
      "examples": [],
      "signature": "(arr: unknown, key: unknown): Record<string, unknown[]>"
    },
    {
      "name": "sort_by",
      "category": "iteration",
      "summary": "Stable sort by field value.",
      "description": "Stable sort by field value. `dir` is 'asc' (default) or 'desc'.\n  • `$sort_by(items, 'price_egp')`         → ascending\n  • `$sort_by(items, 'created_at', 'desc')` → newest first\nReturns a new array; doesn't mutate the input.",
      "examples": [],
      "signature": "(arr: unknown, key: unknown, dir?: unknown): unknown[]"
    },
    {
      "name": "unique",
      "category": "iteration",
      "summary": "Dedupe elements.",
      "description": "Dedupe elements. By default, primitive values are compared by equality;\nwith a `key`, dedupes by `item[key]` (whole objects kept on first occurrence).\n  • `$unique([1, 2, 2, 3])`               → [1, 2, 3]\n  • `$unique(compounds, 'name_en')`       → first compound per name",
      "examples": [],
      "signature": "(arr: unknown, key?: unknown): unknown[]"
    },
    {
      "name": "flat_map",
      "category": "iteration",
      "summary": "Map + flatten one level.",
      "description": "Map + flatten one level. Same mapping semantics as `$map`.\n  • `$flat_map(orders, 'items')`          → all line items across all orders",
      "examples": [],
      "signature": "(arr: unknown, mapping: unknown): unknown[]"
    },
    {
      "name": "count_by",
      "category": "iteration",
      "summary": "Count occurrences of each unique value of `key` (or dotted path).",
      "description": "Count occurrences of each unique value of `key` (or dotted path).\n  • `$count_by(leads, 'status')`          → { 'open': 5, 'closed': 3 }\nReturns {} for non-array / non-string-key inputs.",
      "examples": [],
      "signature": "(arr: unknown, key: unknown): Record<string, number>"
    },
    {
      "name": "format_number",
      "category": "string",
      "summary": "Number formatting via `Intl.NumberFormat`.",
      "description": "Number formatting via `Intl.NumberFormat`. Default locale 'en-EG' matches\nthe existing `formatEGP` style in the codebase.\nUsage in YAML: `$format_number($price)` or `$format_number($n, 'ar-EG')`.",
      "examples": [],
      "signature": "(n: unknown, locale?: unknown): string"
    },
    {
      "name": "format_money",
      "category": "string",
      "summary": "Currency formatter via `Intl.NumberFormat` with `style: 'currency'`.",
      "description": "Currency formatter via `Intl.NumberFormat` with `style: 'currency'`.\nUniversal — pack-agnostic; pass any ISO 4217 currency code.\n  $format_money(price, 'EGP')          → 'EGP 1,500,000'\n  $format_money(price, 'USD', 'en-US') → '$1,500,000.00'\nReturns '' for non-finite numbers. Falls back to `${value} ${currency}`\nwhen Intl rejects an unknown currency code (defensive).",
      "examples": [
        "$format_money(price, 'EGP')          → 'EGP 1,500,000'",
        "$format_money(price, 'USD', 'en-US') → '$1,500,000.00'"
      ],
      "signature": "(value: unknown, currency: unknown, locale?: unknown): string"
    },
    {
      "name": "format_money_field",
      "category": "utility",
      "summary": "Currency formatter for a STORED `money` field — the `{ value, offset }` bag an\nXRM `money` field always holds (real amount = `value / offset`, `offset`\nusually 100).",
      "description": "Currency formatter for a STORED `money` field — the `{ value, offset }` bag an\nXRM `money` field always holds (real amount = `value / offset`, `offset`\nusually 100). `$format_money` takes a plain number; THIS takes the stored bag,\nso an author renders `record.fields.<money>` directly without dividing by hand\n(the common trap — passing the bag to `$format_number`/`$format_money` yields\na blank).\n  $format_money_field($item.fields.price, 'EGP')          → 'EGP 1,500,000'\n  $format_money_field($item.fields.price, 'USD', 'en-US') → '$1,500,000.00'\nAlso accepts a plain number (treated as the amount). Returns '' for anything\nthat is neither a finite number nor a `{ value, offset }` bag.",
      "examples": [
        "$format_money_field($item.fields.price, 'EGP')          → 'EGP 1,500,000'",
        "$format_money_field($item.fields.price, 'USD', 'en-US') → '$1,500,000.00'"
      ],
      "signature": "(m: unknown, currency: unknown, locale?: unknown): string"
    },
    {
      "name": "format_relative_date",
      "category": "string",
      "summary": "Relative-time formatter.",
      "description": "Relative-time formatter. Wraps `Intl.RelativeTimeFormat`.\n  $format_relative_date('2026-05-18T00:00:00Z', 'ar', '2026-05-21T00:00:00Z')\n    → 'قبل 3 أيام'\n  $format_relative_date('2026-05-18T00:00:00Z', 'en')\n    → '3 days ago'\n`base` defaults to \"now\"; pass a fixed ISO timestamp for deterministic\ntests / golden snapshots. Picks the largest unit that yields |Δ| ≥ 1.\nReturns '' for unparseable input.",
      "examples": [
        "$format_relative_date('2026-05-18T00:00:00Z', 'ar', '2026-05-21T00:00:00Z')",
        "$format_relative_date('2026-05-18T00:00:00Z', 'en')"
      ],
      "signature": "(iso: unknown, locale?: unknown, base?: unknown): string"
    },
    {
      "name": "pluralize",
      "category": "string",
      "summary": "Locale-aware plural form selection.",
      "description": "Locale-aware plural form selection. Wraps `Intl.PluralRules`.\n  $pluralize(2, { one: 'غرفة', two: 'غرفتان', many: '{count} غرف',\n                  other: '{count} غرفة' }, 'ar')\n    → 'غرفتان'\n  $pluralize(5, { one: '{count} item', other: '{count} items' })\n    → '5 items'\n`forms` is an object keyed by the CLDR plural categories (`zero`,\n`one`, `two`, `few`, `many`, `other`). The selected string is\ninterpolated for `{count}` against the input number. Returns '' for\na non-number count or non-object forms.\nPack authors who want a domain-specific label (e.g. an Arabic\n\"bedrooms\" label) wrap this builtin in a partial that pulls the\nforms object from a locale entry.",
      "examples": [
        "$pluralize(2, { one: 'غرفة', two: 'غرفتان', many: '{count} غرف',",
        "$pluralize(5, { one: '{count} item', other: '{count} items' })"
      ],
      "signature": "(count: unknown, forms: unknown, locale?: unknown): string"
    },
    {
      "name": "enum_lookup",
      "category": "string",
      "summary": "Enum-table lookup.",
      "description": "Enum-table lookup. Returns `map[value]` if present, else `fallback`,\nelse `value` itself. Replaces the inline `TYPE_AR[x] ?? x` pattern.",
      "examples": [],
      "signature": "(value: unknown, map: unknown, fallback?: unknown): unknown"
    },
    {
      "name": "cond",
      "category": "composition",
      "summary": "Multi-way conditional.",
      "description": "Multi-way conditional. Cases evaluated left-to-right; first match wins.\n  `$cond([{ when: $x == 'a', then: 1 },\n          { when: $x == 'b', then: 2 },\n          { default: true,   then: 3 }])`\nBoth `when` and `then` are pre-evaluated by the expression evaluator —\nthe builtin sees materialized booleans and values.",
      "examples": [],
      "signature": "(cases: unknown): unknown"
    },
    {
      "name": "spread",
      "category": "composition",
      "summary": "Spread `base` then conditionally merge each `{when, value}` override\nwhose `when` is truthy.",
      "description": "Spread `base` then conditionally merge each `{when, value}` override\nwhose `when` is truthy. Replaces the `{...base, ...(cond ? add : {})}`\npattern in TS — and the inline-multi-line `assign:` ladders that\naccumulate optional fields in YAML.\n  `$spread($core, [\n    { when: $is_owner,           value: { actions: [...] } },\n    { when: $compound != null,   value: { compound: $compound } },\n  ])`",
      "examples": [],
      "signature": "(base: unknown, overrides?: unknown): Record<string, unknown>"
    },
    {
      "name": "shape",
      "category": "composition",
      "summary": "Project + rename fields from `obj` in one call.",
      "description": "Project + rename fields from `obj` in one call. Replaces the flat-copy\nprimitives (summarize_*, map_to_*) that do nothing but pick + rename.\n  `$shape($compound, {\n    id:      'id',\n    name:    'name_ar',\n    area:    'areas.name_en',           // dotted paths supported\n    display: 'name_ar ?? name_en',      // NB: no expression eval — use a real expr position\n  })`\nValues in the mapping are field-name STRINGS or dotted paths; not\nexpressions. For per-item expressions use `$map` with a similar pattern.",
      "examples": [],
      "signature": "(obj: unknown, mapping: unknown): Record<string, unknown>"
    },
    {
      "name": "array_of",
      "category": "composition",
      "summary": "Build an array with conditional elements.",
      "description": "Build an array with conditional elements. Elements are either values\n(always included) OR `{when, value}` objects (included only when when\nis truthy). Replaces the `[...always, ...(cond ? [maybe] : [])]` idiom.\n  `$array_of([\n    { title: 'Manage', on_select: {...} },\n    { when: $is_owner, value: { title: 'Edit', on_select: {...} } },\n    { title: 'Cancel' },\n  ])`",
      "examples": [],
      "signature": "(items: unknown): unknown[]"
    },
    {
      "name": "coalesce",
      "category": "composition",
      "summary": "Return the first non-null/undefined arg.",
      "description": "Return the first non-null/undefined arg. Replaces nested `??` chains:\n  $coalesce($view.label_ar, $view.label_en, $view.name)\nReturns undefined when every arg is nullish.",
      "examples": [
        "$coalesce($view.label_ar, $view.label_en, $view.name)"
      ],
      "signature": "(...args: unknown[]): unknown"
    },
    {
      "name": "sum",
      "category": "utility",
      "summary": "Sum an array's finite numbers, or — with a field-path arg — sum that field\nacross an array of objects (matches `$map`/`$filter`'s field-name\nconvention).",
      "description": "Sum an array's finite numbers, or — with a field-path arg — sum that field\nacross an array of objects (matches `$map`/`$filter`'s field-name\nconvention). Non-numbers are ignored; a non-array yields 0. The aggregation\nthe `+` operator can't do over a list — e.g. ad-hoc cart-line display\ntotals (authoritative order/cart totals come precomputed from the\nCart/Order capability).\n  $sum([1, 2, 3])              → 6\n  $sum($cart_items, 'line_total') → 149.97",
      "examples": [
        "$sum([1, 2, 3])              → 6",
        "$sum($cart_items, 'line_total') → 149.97"
      ],
      "signature": "(arr: unknown, field?: unknown): number"
    },
    {
      "name": "round",
      "category": "utility",
      "summary": "Round to `digits` decimal places (default 0), half-up — matches JS\n`Math.round`.",
      "description": "Round to `digits` decimal places (default 0), half-up — matches JS\n`Math.round`. Returns the input unchanged when it isn't a finite number\n(so a null/undefined flows through harmlessly under safe-nav).\n  $round(6.25, 1)  → 6.3\n  $round(1234.56)  → 1235",
      "examples": [
        "$round(6.25, 1)  → 6.3",
        "$round(1234.56)  → 1235"
      ],
      "signature": "(n: unknown, digits?: unknown): unknown"
    },
    {
      "name": "min",
      "category": "utility",
      "summary": "Min / max of an array's finite numbers (non-numbers ignored).",
      "description": "Min / max of an array's finite numbers (non-numbers ignored). `null` for\nan empty/all-non-numeric array, so callers can guard with `$coalesce`.\n  $min([3, 1, 2])  → 1   ·   $max($prices)  → 9500000",
      "examples": [
        "$min([3, 1, 2])  → 1   ·   $max($prices)  → 9500000"
      ],
      "signature": "(arr: unknown): number | null"
    },
    {
      "name": "get",
      "category": "utility",
      "summary": "Safe nested lookup with a fallback: `$get(obj, 'a.b.c', fallback)`.",
      "description": "Safe nested lookup with a fallback: `$get(obj, 'a.b.c', fallback)`.\nResolves a dotted path null-safely; returns `fallback` (default null)\nwhen the path is missing/undefined. The intuitive companion to literal\nmember access for dynamic keys + defaults — e.g.\n  `$get($constants.mime_extensions, $m.mime_type, '.bin')`.",
      "examples": [],
      "signature": "(obj: unknown, path: unknown, fallback?: unknown): unknown"
    },
    {
      "name": "is_empty",
      "category": "predicate",
      "summary": "True for null, undefined, '', [], or {} with no own keys.",
      "description": "True for null, undefined, '', [], or {} with no own keys.",
      "examples": [],
      "signature": "(v: unknown): boolean"
    },
    {
      "name": "has_any_key",
      "category": "predicate",
      "summary": "True when `obj` has a defined value for any key in `keys`.",
      "description": "True when `obj` has a defined value for any key in `keys`.",
      "examples": [],
      "signature": "(obj: unknown, keys: unknown): boolean"
    },
    {
      "name": "has_all_keys",
      "category": "predicate",
      "summary": "True when `obj` has a defined value for every key in `keys`.",
      "description": "True when `obj` has a defined value for every key in `keys`.",
      "examples": [],
      "signature": "(obj: unknown, keys: unknown): boolean"
    },
    {
      "name": "keys",
      "category": "utility",
      "summary": "Object.keys, defensively.",
      "description": "Object.keys, defensively.",
      "examples": [],
      "signature": "(v: unknown): string[]"
    },
    {
      "name": "length",
      "category": "utility",
      "summary": "Length for arrays/strings, key count for objects, 0 otherwise.",
      "description": "Length for arrays/strings, key count for objects, 0 otherwise.",
      "examples": [],
      "signature": "(v: unknown): number"
    },
    {
      "name": "merge",
      "category": "utility",
      "summary": "Shallow object merge (b's keys override a's).",
      "description": "Shallow object merge (b's keys override a's). null-safe.",
      "examples": [],
      "signature": "(a: unknown, b: unknown): Record<string, unknown>"
    },
    {
      "name": "pick",
      "category": "utility",
      "summary": "Pick a subset of keys from an object.",
      "description": "Pick a subset of keys from an object.",
      "examples": [],
      "signature": "(obj: unknown, keys: unknown): Record<string, unknown>"
    },
    {
      "name": "omit",
      "category": "utility",
      "summary": "Drop a set of keys from an object (inverse of `pick`).",
      "description": "Drop a set of keys from an object (inverse of `pick`). null-safe.",
      "examples": [],
      "signature": "(obj: unknown, keys: unknown): Record<string, unknown>"
    },
    {
      "name": "entries",
      "category": "utility",
      "summary": "Object → array of `{ key, value }` pairs (insertion order).",
      "description": "Object → array of `{ key, value }` pairs (insertion order). Inverse-ish of\nbuilding an object; pairs nicely with `$group_by` to make sections.\n  $entries($group_by($items, 'area'))  → [{ key: 'New Cairo', value: [...] }, …]\nReturns [] for non-object inputs.",
      "examples": [
        "$entries($group_by($items, 'area'))  → [{ key: 'New Cairo', value: [...] }, …]"
      ],
      "signature": "(obj: unknown): Array<{ key: string; value: unknown }>"
    },
    {
      "name": "keys_where",
      "category": "iteration",
      "summary": "Filter object KEYS by their values' truthiness.",
      "description": "Filter object KEYS by their values' truthiness.\n  • `$keys_where({ a: 1, b: 0, c: 'x' })`  → ['a', 'c']\nReturns [] for non-object inputs.",
      "examples": [],
      "signature": "(obj: unknown): string[]"
    },
    {
      "name": "paginate",
      "category": "utility",
      "summary": "Window an array into `{ page, total, showing_offset, has_more, next_offset }`.",
      "description": "Window an array into `{ page, total, showing_offset, has_more, next_offset }`.\n  • `$paginate(items, 0, 10)`  → first 10 + cursor\n  • `$paginate(items, 10, 10)` → next 10 + cursor\nNon-array input → an empty page (total 0, no more). `offset`/`limit`\ncoerce to safe non-negative numbers; a non-positive `limit` yields an\nempty page while preserving `total` (callers can still show \"0 shown of N\").",
      "examples": [],
      "signature": "(items: unknown, offset: unknown, limit: unknown): PageEnvelope"
    },
    {
      "name": "matches",
      "category": "utility",
      "summary": "Regex test.",
      "description": "Regex test. `$matches(text, pattern, flags?)` → boolean.\n  $matches($msg, '[0-9]{7,}')            → true when 7+ consecutive digits\n  $matches($msg, 'foo', 'i')             → case-insensitive\nNon-string text / pattern or an invalid pattern → false.",
      "examples": [
        "$matches($msg, '[0-9]{7,}')            → true when 7+ consecutive digits",
        "$matches($msg, 'foo', 'i')             → case-insensitive"
      ],
      "signature": "(text: unknown, pattern: unknown, flags?: unknown): boolean"
    },
    {
      "name": "count",
      "category": "utility",
      "summary": "Count regex matches.",
      "description": "Count regex matches. `$count(text, pattern, flags?)` → number. Always\ncounts globally (the `g` flag is added if absent).\n  $count($msg, '[0-9٠-٩]')     → how many real digits in the text",
      "examples": [
        "$count($msg, '[0-9٠-٩]')     → how many real digits in the text"
      ],
      "signature": "(text: unknown, pattern: unknown, flags?: unknown): number"
    },
    {
      "name": "normalize_digits",
      "category": "utility",
      "summary": "Fold non-ASCII decimal digits to ASCII 0-9 (Arabic-Indic ٠-٩ + Persian\n۰-۹).",
      "description": "Fold non-ASCII decimal digits to ASCII 0-9 (Arabic-Indic ٠-٩ + Persian\n۰-۹). `$normalize_digits('٠١٢')` → '012'. Other characters pass through.",
      "examples": [],
      "signature": "(text: unknown): unknown"
    },
    {
      "name": "translate",
      "category": "utility",
      "summary": "1:1 character translation (SQL-`translate` style).",
      "description": "1:1 character translation (SQL-`translate` style). Each char in `from` is\nreplaced by the char at the same index in `to`; chars absent from `from`\npass through. Used for leet-speak folding, diacritic stripping, etc.\n  $translate($msg, 'OolI', '0011')       → fold O/o→0, l/I→1\n`to` shorter than `from`: the unmatched `from` chars map to '' (deleted).",
      "examples": [
        "$translate($msg, 'OolI', '0011')       → fold O/o→0, l/I→1"
      ],
      "signature": "(text: unknown, from: unknown, to: unknown): unknown"
    },
    {
      "name": "truncate",
      "category": "string",
      "summary": "Truncate a string to at most `max` characters, appending `ellipsis`\n(default '…') when truncation happens.",
      "description": "Truncate a string to at most `max` characters, appending `ellipsis`\n(default '…') when truncation happens. Returns the string unchanged\nwhen shorter than `max`.\n  $truncate('long title here', 8)       → 'long ti…'\n  $truncate('short', 8)                 → 'short'\n  $truncate('long', 8, '...')           → 'long'    (no trunc needed)\n  $truncate('very long title', 8, '...') → 'very ...'\nReturns '' for non-string input. The ellipsis counts against `max`, so\nthe output is never longer than `max`.",
      "examples": [
        "$truncate('long title here', 8)       → 'long ti…'",
        "$truncate('short', 8)                 → 'short'",
        "$truncate('long', 8, '...')           → 'long'    (no trunc needed)",
        "$truncate('very long title', 8, '...') → 'very ...'"
      ],
      "signature": "(str: unknown, max: unknown, ellipsis?: unknown): string"
    },
    {
      "name": "summarize_items",
      "category": "string",
      "summary": "Summarize an array as a single-line breadcrumb — universal helper for\nmemory_note fields.",
      "description": "Summarize an array as a single-line breadcrumb — universal helper for\nmemory_note fields. Each element renders the `template` with `{path}`\nplaceholders resolved against the element via dotted path lookup.\n  $summarize_items($views, '{reference} {city} [{status}]',\n    { max: 5, separator: ' | ' })\n    → 'UID-A city1 [available] | UID-B city2 [sold] | …'\nDefaults: `max = 5`, `separator = ' | '`. Returns '' for non-array\ninputs. Path lookups are null-safe; missing paths render as ''.\nNOTE: the template uses bare `{path}` (NOT `{$item.path}`) because\nexpressions are already evaluated by the time the builtin runs. The\nper-item substitution happens inside this function against each item.",
      "examples": [
        "$summarize_items($views, '{reference} {city} [{status}]',"
      ],
      "signature": "(arr: unknown, template: unknown, opts?: unknown): string"
    },
    {
      "name": "unit_ref",
      "category": "domain",
      "summary": "UUID → `UID-XXXXXXXX` reference string.",
      "description": "UUID → `UID-XXXXXXXX` reference string. Mirrors `utils/ref.ts:unitRef`.\nPure string slicing — safe to use inline in YAML expressions.",
      "examples": [],
      "signature": "(uuid: unknown): string"
    },
    {
      "name": "lead_ref",
      "category": "domain",
      "summary": "UUID → `LID-XXXXXXXX` lead reference.",
      "description": "UUID → `LID-XXXXXXXX` lead reference. Mirrors `utils/ref.ts:leadRef`.",
      "examples": [],
      "signature": "(uuid: unknown): string"
    },
    {
      "name": "lines",
      "category": "string",
      "summary": "Join parts into a single string, filtering only null and undefined.",
      "description": "Join parts into a single string, filtering only null and undefined.\nEmpty strings are PRESERVED — they act as intentional layout separators\n(e.g. blank lines between sections of a card body). Default separator\nis '\\n'. Replaces the `const lines: string[] = []; if (x) lines.push(...);\nreturn lines.join('\\n')` pattern from primitives — pure transforms\nbecome inline YAML. Phase 6 Win A; semantics relaxed in Phase 8 so\nformatter migrations preserve empty-line layout.\nUsage in YAML:\n  `'$lines([$header, $body, $footer])'`              → '\\n' separated\n  `'$lines($parts, \" | \")'`                          → custom separator\n  `'$lines([$a, ($b > 0 ? $b_label : null)])'`       → conditional segments (null is dropped)\n  `'$lines([$header, \"\", $body])'`                   → blank line between header and body\nTo drop empty strings too (legacy Phase-6 behavior), filter the array\nbefore passing: `$lines($parts.filter(p => p))`.",
      "examples": [],
      "signature": "(parts: unknown, sep?: unknown): string"
    },
    {
      "name": "url_encode",
      "category": "utility",
      "summary": "URL-encode a string (encodeURIComponent) — e.g.",
      "description": "URL-encode a string (encodeURIComponent) — e.g. building wa.me share links.",
      "examples": [],
      "signature": "(s: unknown): string"
    },
    {
      "name": "trim",
      "category": "utility",
      "summary": "Trim leading/trailing whitespace.",
      "description": "Trim leading/trailing whitespace. Returns '' for non-string input.",
      "examples": [],
      "signature": "(s: unknown): string"
    },
    {
      "name": "lower",
      "category": "utility",
      "summary": "Lowercase a string (locale-agnostic).",
      "description": "Lowercase a string (locale-agnostic). Returns '' for non-string input.",
      "examples": [],
      "signature": "(s: unknown): string"
    },
    {
      "name": "upper",
      "category": "utility",
      "summary": "Uppercase a string (locale-agnostic).",
      "description": "Uppercase a string (locale-agnostic). Returns '' for non-string input.",
      "examples": [],
      "signature": "(s: unknown): string"
    },
    {
      "name": "join_with",
      "category": "string",
      "summary": "Join an array into a human-readable list with separator + optional\n\"and\" connector for the last element:\n  $join_with(['a', 'b', 'c'], ', ', ', and ')   → 'a, b, and c'\n  $join_with(['only one'], ', ', ', and ')      → 'only one'\n  $join_with(['a', 'b'], ', ', ' and ')         → 'a and b'\nWhen `last_sep` is omitted, behaves like `Array.prototype.join`.",
      "description": "Join an array into a human-readable list with separator + optional\n\"and\" connector for the last element:\n  $join_with(['a', 'b', 'c'], ', ', ', and ')   → 'a, b, and c'\n  $join_with(['only one'], ', ', ', and ')      → 'only one'\n  $join_with(['a', 'b'], ', ', ' and ')         → 'a and b'\nWhen `last_sep` is omitted, behaves like `Array.prototype.join`.\nNull / undefined elements are dropped; remaining values stringified.",
      "examples": [
        "$join_with(['a', 'b', 'c'], ', ', ', and ')   → 'a, b, and c'",
        "$join_with(['only one'], ', ', ', and ')      → 'only one'",
        "$join_with(['a', 'b'], ', ', ' and ')         → 'a and b'"
      ],
      "signature": "(arr: unknown, sep: unknown, last_sep?: unknown): string"
    },
    {
      "name": "first_n",
      "category": "iteration",
      "summary": "First N WORDS of a string (vs `$truncate`'s char-based cut).",
      "description": "First N WORDS of a string (vs `$truncate`'s char-based cut). Splits\non whitespace runs; preserves single-space joining. Appends `ellipsis`\n(default '…') when truncated.\n  $first_n('the quick brown fox jumps', 3)        → 'the quick brown…'\n  $first_n('short', 5)                            → 'short'\n  $first_n('', 3)                                 → ''",
      "examples": [
        "$first_n('the quick brown fox jumps', 3)        → 'the quick brown…'",
        "$first_n('short', 5)                            → 'short'",
        "$first_n('', 3)                                 → ''"
      ],
      "signature": "(str: unknown, n: unknown, ellipsis?: unknown): string"
    },
    {
      "name": "slug",
      "category": "string",
      "summary": "Make a URL-safe slug from a string.",
      "description": "Make a URL-safe slug from a string. Lowercases ASCII, collapses\nruns of non-alphanumeric chars to `-`, trims leading/trailing `-`.\nPreserves Unicode letters/digits (so `سلام عليكم` → `سلام-عليكم`).\n  $slug('Hello World')           → 'hello-world'\n  $slug('  Foo --- Bar! ')       → 'foo-bar'\n  $slug('سلام عليكم')             → 'سلام-عليكم'\n  $slug('')                      → ''",
      "examples": [
        "$slug('Hello World')           → 'hello-world'",
        "$slug('  Foo --- Bar! ')       → 'foo-bar'",
        "$slug('سلام عليكم')             → 'سلام-عليكم'",
        "$slug('')                      → ''"
      ],
      "signature": "(s: unknown): string"
    },
    {
      "name": "t",
      "category": "runtime-bound",
      "summary": "Pack-locale string lookup for the run's language. Keys MUST be namespaced (`<pack_id>.…` / `<flow_id>.…`) — a bare key returns the raw string unchanged (lint-enforced).",
      "description": "Pack-locale string lookup for the run's language. Keys MUST be namespaced (`<pack_id>.…` / `<flow_id>.…`) — a bare key returns the raw string unchanged (lint-enforced). (Runtime-bound: provided per run by FlowTool (locale/pack context), not an evaluator builtin.)",
      "examples": [
        "$t('clinic.booking.confirmed')",
        "$t('ticket-status.reply_prompt', { name: $contact.display_name })"
      ],
      "signature": "(key: string, params?: Record<string, unknown>) => string",
      "kind": "runtime-bound"
    },
    {
      "name": "t_has",
      "category": "runtime-bound",
      "summary": "True when the pack locale defines the (namespaced) key for the run's language — guard optional copy before `$t`.",
      "description": "True when the pack locale defines the (namespaced) key for the run's language — guard optional copy before `$t`. (Runtime-bound: provided per run by FlowTool (locale/pack context), not an evaluator builtin.)",
      "examples": [
        "$t_has('clinic.booking.followup_hint')"
      ],
      "signature": "(key: string) => boolean",
      "kind": "runtime-bound"
    },
    {
      "name": "enum_label",
      "category": "runtime-bound",
      "summary": "Auto-namespaced enum label: resolves `$t(\"<pack>.<category>.<value>\")` — the pack-level enum-label convention; falls back to `fallback` then the raw value.",
      "description": "Auto-namespaced enum label: resolves `$t(\"<pack>.<category>.<value>\")` — the pack-level enum-label convention; falls back to `fallback` then the raw value. (Runtime-bound: provided per run by FlowTool (locale/pack context), not an evaluator builtin.)",
      "examples": [
        "$enum_label($item.type, 'types')",
        "$enum_label($case.status, 'case_status', $case.status)"
      ],
      "signature": "(value: string, category: string, fallback?: string) => string",
      "kind": "runtime-bound"
    },
    {
      "name": "loc",
      "category": "runtime-bound",
      "summary": "Resolve a LOCALIZED xrm field value (`{ en?, ar? }`) to one display string: viewer locale → en → ar → first string value. Scalars pass through untouched.",
      "description": "Resolve a LOCALIZED xrm field value (`{ en?, ar? }`) to one display string: viewer locale → en → ar → first string value. Scalars pass through untouched. (Runtime-bound: provided per run by FlowTool (locale/pack context), not an evaluator builtin.)",
      "examples": [
        "$loc($item.fields.name)"
      ],
      "signature": "(value: unknown) => unknown",
      "kind": "runtime-bound"
    },
    {
      "name": "record_url",
      "category": "runtime-bound",
      "summary": "Absolute public page URL for an XRM record (`GET /r/:recordId`; the entity must declare `public:` in xrm.yaml). Empty string for a non-string/empty id.",
      "description": "Absolute public page URL for an XRM record (`GET /r/:recordId`; the entity must declare `public:` in xrm.yaml). Empty string for a non-string/empty id. (Runtime-bound: provided per run by FlowTool (locale/pack context), not an evaluator builtin.)",
      "examples": [
        "$record_url($item.id)"
      ],
      "signature": "(record_id: string) => string",
      "kind": "runtime-bound"
    },
    {
      "name": "work_types",
      "category": "runtime-bound",
      "summary": "The pack's declared work sub-type ids for an entity (its `worklist.yaml` `work.<entity>.types` keys), optionally filtered to types routed by the given field. `[]` when the pack works no such entity.",
      "description": "The pack's declared work sub-type ids for an entity (its `worklist.yaml` `work.<entity>.types` keys), optionally filtered to types routed by the given field. `[]` when the pack works no such entity. (Runtime-bound: provided per run by FlowTool (locale/pack context), not an evaluator builtin.)",
      "examples": [
        "$work_types('case')",
        "$work_types('case', 'region')"
      ],
      "signature": "(entity: string, route_by?: string) => string[]",
      "kind": "runtime-bound"
    },
    {
      "name": "partial",
      "category": "runtime-bound",
      "summary": "Invoke a registered render partial by name (resolved against the pack's partial registry; needs ExprContext).",
      "description": "Invoke a registered render partial by name (resolved against the pack's partial registry; needs ExprContext). (Runtime-bound: provided per run by the evaluator (needs ExprContext), not an evaluator builtin.)",
      "examples": [
        "$partial('price_line', $item)"
      ],
      "signature": "(name: string, arg?: unknown) => unknown",
      "kind": "runtime-bound"
    },
    {
      "name": "diff_lines",
      "category": "runtime-bound",
      "summary": "Generic before→after diff body — one line per changed key, labels via `$t`/`$enum_label` (needs ExprContext). Powers edit-confirmation cards.",
      "description": "Generic before→after diff body — one line per changed key, labels via `$t`/`$enum_label` (needs ExprContext). Powers edit-confirmation cards. (Runtime-bound: provided per run by the evaluator (needs ExprContext), not an evaluator builtin.)",
      "examples": [
        "$diff_lines($current, $state, { label_prefix: $pack_id })"
      ],
      "signature": "(before: object, after: object, opts?: { header?: string; confirm?: string; label_prefix?: string; enum?: Record<string, string>; skip?: string[] }) => string[]",
      "kind": "runtime-bound"
    }
  ]
}
